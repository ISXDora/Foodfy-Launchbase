<div class="items">
    <div class="item">
        <div>Imagem da receita</div>
        <div>
            <input
                type="url"
                name="image"
                placeholder="http://"
                value="{{recipe.image}}"
            />
        </div>
    </div>

    <div class="item">
        <div>Nome da receita</div>
        <div>
            <input
                type="text"
                name="title"
                placeholder="Nome da receita"
                value="{{recipe.title}}"
            />
        </div>
    </div>

    <div class="item">
        <div>Chef</div>
        <div>
            <select name="chef"
                    placeholder="Selecione um chef">
                {% for chef in chefsOptions %}
                    <option
                        
                        value="{{chef.id}}" {%if recipe.chef_id == chef.id %}
                        selected {%endif%} >{{chef.name }}</option>
                {% endfor %}
                    <option                         
                        value="" {%if recipe.chef_id == chef.id %}
                        selected {%endif%} >Selecione um chef</option>
            </select>                    
        </div>
    </div>

    <div class="item">
        <div id="ingredients">Ingredientes</div>
        {% for item in recipe.ingredients%}
        <div class="ingredient">
            <input
                type="text"
                name="ingredients"
                placeholder="Insira um ingrendiente de cada vez"
                value="{{item}}"
                   
            />
        </div>
        {% else %}
        <div class="ingredient">
            <input
                type="text"
                name="ingredients"
                placeholder="Insira um ingrendiente de cada vez"
                value=""      
            />
        </div>
        {% endfor%}
        <button type="button" class="add-ingredient">Adicionar novo ingrediente</button>
    </div>

    <div class="item">
        <div id="preparations">Modo de preparo</div>
        {% for item in recipe.preparations%}
        <div class="preparation">
            <input
                type="text"
                name="preparation"
                placeholder="Insira a forma de preparo passo a passo"
                value="{{item}}"
            />
        </div>
        {% else %}
        <div class="preparation">
            <input
                type="text"
                name="preparation"
                placeholder="Insira a forma de preparo passo a passo"
                value=""
            />
        </div>
        {%endfor%}
        <button type="button" class="add-preparation">Adicionar novo passo</button>              
    </div>

    <div class="item">
        <div >Informações Adicionais</div>
        <div>
            <textarea
                type="text"
                name="information"
            >{{recipe.information}}
            </textarea>
        </div>
        
    </div>

</div>